using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Microsoft.LightSwitch;
using Microsoft.LightSwitch.Security.Server;
using System.Linq.Expressions;

namespace LightSwitchApplication
{
    public partial class ApplicationDataService
    {

        //partial void SaveChanges_CanExecute(ref bool result)
        //{
        //    result = (this.Application.User.HasPermission(Permissions.WriteMode) || this.Application.User.HasPermission(Permissions.SecurityAdministration));
        //}

        //partial void Sprints_CanInsert(ref bool result)
        //{
        //    result = (this.Application.User.HasPermission(Permissions.WriteMode) || this.Application.User.HasPermission(Permissions.SecurityAdministration));
        //}

        //partial void Sprints_CanUpdate(ref bool result)
        //{
        //    result = (this.Application.User.HasPermission(Permissions.WriteMode) || this.Application.User.HasPermission(Permissions.SecurityAdministration));
        //}

       
    }
}
